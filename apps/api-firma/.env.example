# ===========================================
# Variables de entorno para FacturaOn API
# ===========================================
# Copia este archivo a .env y completa los valores
# cp .env.example .env

# ----- Base de Datos -----
# Local (Docker apps/db + red fon-net):
DB_HOST=fon-postgres
DB_PORT=5432
DB_NAME=fon23_dev
DB_USERNAME=fon
DB_PASSWORD=fon_local_dev

# Remoto (solo si no usas PostgreSQL local):
# DB_HOST=107.20.84.190
# DB_USERNAME=user_fon23_dev
# DB_PASSWORD=tu_password_aqui

# Base de datos de test (opcional, para pruebas locales)
DB_NAME_TEST=fon23_test

# ----- Rails -----
RAILS_ENV=development
RAILS_MAX_THREADS=5

# ----- JWT -----
# Clave secreta para firmar tokens (genera una con: rails secret)
# Si no se define, usa Rails credentials.secret_key_base
# JWT_SECRET_KEY=tu_clave_secreta_muy_larga_aqui

# ----- CORS -----
# Orígenes permitidos separados por coma
# En desarrollo: * permite todos
# En producción: especificar dominios
CORS_ORIGINS=*

# ----- Opcional: URL completa de BD -----
# Si prefieres usar DATABASE_URL en lugar de variables separadas:
# DATABASE_URL=postgres://user_fon23_dev:password@107.20.84.190:5432/fon23_dev

# ----- Usuarios de personas autorizadas -----
# Password temporal al crear usuario de persona autorizada (opcional).
# Debe cumplir la política: 8+ chars, mayúscula, minúscula y número.
# Si falta o no cumple, se genera una contraseña temporal automáticamente.
PERSONA_AUTORIZADA_DEFAULT_PASSWORD=Temporal123

# ----- Correo / onboarding -----
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_DOMAIN=gmail.com
SMTP_USERNAME=traroenrolamiento@gmail.com
SMTP_PASSWORD=tu_app_password_de_gmail
SMTP_AUTHENTICATION=plain
SMTP_ENABLE_STARTTLS=true

MAIL_FROM=traroenrolamiento@gmail.com
MAIL_FROM_NAME=FacturaOn Enrolamiento

# Logo embebido en correos (recomendado; funciona en Gmail sin URL pública)
# Archivo servido desde api-firma/public/brand/logo-email-white.png
# Si actualizas el logo en web-arribo, copia también a esa ruta.
# MAIL_LOGO_FILE=public/brand/logo-email-white.png

# URL externa del logo (opcional). Solo si prefieres no embeberlo.
# Debe ser HTTPS pública; localhost NO funciona en Gmail.
# MAIL_LOGO_URL=https://tu-dominio.cl/brand/logo-email-white.png

# URL del frontend para links en correos de onboarding
FRONTEND_BASE_URL=http://localhost:5173

# Horas de validez del token enviado por correo
ONBOARDING_TOKEN_EXPIRY_HOURS=48

# Horas de validez del enlace de recuperación de contraseña
PASSWORD_RESET_TOKEN_EXPIRY_HOURS=24

# Timeouts SMTP en segundos (Gmail puede tardar desde Docker/red lenta)
SMTP_OPEN_TIMEOUT=30
SMTP_READ_TIMEOUT=30

# ----- SII (Servicio de Impuestos Internos) -----
# Ambiente: 'certificacion' o 'produccion'
SII_AMBIENTE=certificacion
